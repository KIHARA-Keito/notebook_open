# 認証・認可の学習ノート

Web サービスの「認証（Authentication）」と「認可（Authorization）」を実装できるようになるための学習メモ。
概念 → 手を動かす → ライブラリで再実装、の順で進めると挫折しにくい。

---

## 0. まず用語を整理

| 用語                  | 意味                               | ひとことで       |
| --------------------- | ---------------------------------- | ---------------- |
| 認証 Authentication   | 「誰であるか」を確認する           | ログイン         |
| 認可 Authorization    | 「何をしてよいか」を判定する       | 権限・ロール     |
| Session / Cookie      | サーバー側で状態を持つ古典方式     | サーバーが覚える |
| Token / JWT           | 状態をトークン自体に持たせる方式   | トークンが証明書 |
| OAuth 2.0             | 他サービスへの「アクセス権の委譲」 | 代理で権限を渡す |
| OIDC (OpenID Connect) | OAuth の上に乗せた「本人確認」     | 〇〇でログイン   |

- この 5 つの違いを自分の言葉で説明できるのが第一関門。
- 認証と認可は別物。ログインできても（認証 OK）、管理画面は見れない（認可 NG）は普通に起きる。

---

## 1. セッション方式（まず基本）

```
1. ユーザーが ID / PW を送信
2. サーバーが検証し、セッションID を発行
3. セッションID を Cookie に入れて返す
4. 以降のリクエストは Cookie を自動送信 → サーバーが照合
```

- パスワードは必ずハッシュ化して保存（bcrypt / argon2）。平文・可逆暗号は NG。
- Cookie には `HttpOnly`（JS からアクセス不可）、`Secure`（HTTPS のみ）、`SameSite` を付ける。
- 状態はサーバー側（DB / Redis 等）に持つ → スケールやログアウトの即時無効化がしやすい。

## 2. トークン方式（JWT）

```
1. ログイン成功時にアクセストークン（短命）とリフレッシュトークン（長命）を発行
2. クライアントは API 呼び出し時に Authorization: Bearer <token> を付与
3. サーバーは署名を検証するだけで本人確認（DB照合不要 = ステートレス）
4. アクセストークンが切れたらリフレッシュトークンで再発行
```

- JWT = ヘッダ.ペイロード.署名 の 3 パート。**署名で改ざん検知はできるが、中身は誰でも読める**（暗号化ではない）。
- なので JWT にパスワードや個人情報は入れない。
- 欠点：発行済みトークンは即時に無効化しにくい → 有効期限を短く + リフレッシュで運用。
- jwt.io で実際にデコードして中身を見ると理解が早い。

## 3. OAuth 2.0 / OIDC（〇〇でログイン）

- OAuth 2.0 = **認可**の仕組み（例：「このアプリに Google Drive の読み取りを許可」）。
- OIDC = OAuth の上に**認証**を乗せた規格（例：「Google アカウントでログイン」）。
- 実装で覚えるべきは **Authorization Code Flow**：
  ```
  アプリ → 認可サーバーへリダイレクト → ユーザー同意 → 認可コード発行
       → アプリがコードをトークンに交換 → ユーザー情報取得
  ```
- 一次情報：RFC 6749 (OAuth 2.0) / OpenID Connect Core。まずは軽く目を通す程度で OK。

---

## 4. 手を動かす（ここが本番）

読むだけでは身につかないので、最小構成を自分で実装する。段階を踏む：

1. **パスワード認証をゼロから** — ハッシュ化、セッション Cookie 発行、ログイン/ログアウト
2. **JWT に置き換え** — アクセス + リフレッシュトークンの流れ
3. **OAuth/OIDC を体験** — 「Google でログイン」を 1 つ実装
4. **ライブラリで再実装** — 自作の後に使うと「何を隠蔽しているか」が腹落ちする

→ 自作を先にやると、ライブラリのありがたみと危険性の両方が分かる。

## 5. 主要ライブラリ / サービス（Next.js 前提）

- **NextAuth (Auth.js)** — Next.js 定番。OAuth プロバイダ連携がラク。※ 00_TODO.md にも記載あり
- **Clerk** — UI 込みで最速。ユーザー管理画面まで付属。※ 00_TODO.md にも記載あり
- **Supabase Auth** — DB とセットで使うなら。
- **Auth0** — 大規模・エンプラ向けの定番 IDaaS。
- まず 1 つ（NextAuth か Clerk）で「Google ログイン + 保護ページ」を作るのがおすすめ。

## 6. 認可（ログインの先）

- **RBAC**（Role-Based Access Control）：admin / editor / viewer などロールで制御。まずこれ。
- **ABAC**（Attribute-Based）：ユーザー属性やリソース所有者で細かく制御。
- 実装：ミドルウェアやサーバー側で「このユーザーはこの操作を許可されているか」を毎回チェック。
- フロントの出し分けは UX 用。**本当の防御は必ずサーバー側**で行う。

---

## 参考リンク

- OWASP Authentication Cheat Sheet（実装時の「やってはいけない」集。必読）
  https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html
- The Copenhagen Book（セッション/トークン/PW 管理を実装目線で。無料）
  https://thecopenhagenbook.com/
- jwt.io（JWT デコード体験）
  https://jwt.io/
- Auth.js (NextAuth) 公式 https://authjs.dev/
- Clerk 公式 https://clerk.com/docs

## 次にやること TODO

- [ ] 用語 5 つを自分の言葉で説明できるか確認
- [ ] Next.js + NextAuth で Google ログイン + 保護ページを 1 本作る
- [ ] JWT を jwt.io でデコードして中身を観察
- [ ] RBAC で admin/一般ユーザーの出し分けを実装
