# 認証・認可 学習プラン（初心者向け）

Web サービス開発における「認証（Authentication）」「認可（Authorization）」を、
用語の理解 → 手を動かす → 実サービスに載せる、の順で学ぶためのプラン。

想定スタック: Next.js (App Router) + TypeScript
想定期間: 週 5〜8 時間 × 約 10 週間（Step 0〜7）

---

## 前提: 用語の整理

| 用語                       | 意味                             | 例                                 |
| -------------------------- | -------------------------------- | ---------------------------------- |
| 認証 (Authentication, AuthN) | 「誰であるか」を確かめる         | ログイン画面でメール+パスワード     |
| 認可 (Authorization, AuthZ)  | 「何をしてよいか」を決める       | 管理者だけが記事を削除できる       |
| セッション                 | ログイン状態をサーバ側で保持する | Cookie にセッション ID を持たせる  |
| トークン                   | ログイン状態を署名付き文字列で持つ | JWT (JSON Web Token)               |
| ID 連携 (フェデレーション)  | 他サービスの認証結果を借りる     | 「Google でログイン」              |

まず押さえるべき原則:

- 認証と認可は**別のレイヤー**。混ぜて実装すると必ず穴が空く。
- 認可は**必ずサーバ側**で判定する。フロントの出し分けは UX であってセキュリティではない。
- パスワードは**平文で保存しない・ログに出さない・自前でハッシュ方式を発明しない**。

---

## Step 0. HTTP とステートレスの理解（3〜4 時間）

認証が難しい根本理由は「HTTP がステートレス（前のリクエストを覚えていない）」だから。
ここが腹落ちしていないと Cookie もトークンも丸暗記になる。

- [ ] リクエスト / レスポンス、ヘッダ、ステータスコード（200 / 301 / 401 / 403）
- [ ] 401 Unauthorized（未認証）と 403 Forbidden（認可されていない）の違い
- [ ] Cookie の仕組み（Set-Cookie ヘッダ → 次回リクエストで自動送信）
- [ ] ブラウザ DevTools の Network / Application タブで Cookie を実際に見る

**課題**: 適当なサイトにログインして、DevTools で Cookie がどう増えるか観察しメモする。

参考: MDN「HTTP Cookie の使用」

---

## Step 1. パスワード認証の基礎（4〜6 時間）

- [ ] ハッシュ化とは何か（暗号化との違い＝ハッシュは元に戻せない）
- [ ] ソルト (salt) が必要な理由（同じパスワード → 同じハッシュを防ぐ）
- [ ] bcrypt / argon2 といった「わざと遅い」ハッシュ関数を使う理由
- [ ] パスワードリセットの安全な流れ（ワンタイムトークン + 有効期限）

**課題**: Node.js で `bcrypt` を使い、ハッシュ生成と照合 (`compare`) を動かす。

```bash
npm install bcrypt
```

```ts
import bcrypt from "bcrypt";

const hash = await bcrypt.hash("password123", 10);
const ok = await bcrypt.compare("password123", hash); // true
```

---

## Step 2. セッション認証を自作する（8〜10 時間）★ 最重要

いきなりライブラリを使わず、一度**自分で作る**と中身が分かる。

- [ ] ログイン成功 → セッション ID を発行 → Cookie に保存
- [ ] サーバ側（DB / メモリ）でセッション ID とユーザ ID を紐付ける
- [ ] リクエストごとに Cookie からセッションを引いてユーザを特定
- [ ] ログアウト → サーバ側のセッションを破棄する（Cookie 削除だけでは不十分）

Cookie に必ず付ける属性:

| 属性                | 目的                                             |
| ------------------- | ------------------------------------------------ |
| `HttpOnly`          | JavaScript から読めなくする（XSS 対策）          |
| `Secure`            | HTTPS でのみ送信                                 |
| `SameSite=Lax/Strict` | 他サイトからの送信を制限（CSRF 対策）          |
| `Max-Age` / `Expires` | 有効期限                                       |

**課題**: Express または Next.js Route Handler で、
「/login」「/logout」「/me（ログイン中のみ 200、それ以外 401）」の 3 本を自作する。

---

## Step 3. トークン認証と JWT（6〜8 時間）

- [ ] JWT の構造（header.payload.signature）を jwt.io で分解して見る
- [ ] 署名は「改ざん検知」であって「暗号化」ではない（payload は誰でも読める）
- [ ] アクセストークン（短命）とリフレッシュトークン（長命）の役割分担
- [ ] JWT の弱点＝**即時失効させにくい**（だから有効期限を短くする）

セッション vs JWT の使い分け:

| 観点           | セッション                 | JWT                          |
| -------------- | -------------------------- | ---------------------------- |
| 状態の保持     | サーバ側                   | トークン自体                 |
| 失効           | 簡単（DB から消すだけ）    | 難しい（期限切れ待ち）       |
| スケール       | セッションストアが必要     | サーバ側が状態を持たなくて済む |
| 向いている用途 | 通常の Web アプリ          | API / マイクロサービス間     |

**判断基準**: 普通の Web サービスなら、まず**セッション（Cookie）で十分**。
「JWT のほうが今風だから」で選ばない。

**課題**: `jose` などで JWT を発行 / 検証し、payload を base64 デコードして中身を覗く。

---

## Step 4. OAuth 2.0 / OpenID Connect（8〜12 時間）

「Google でログイン」の中身。ここは概念が多いので図を描きながら進める。

- [ ] 登場人物（リソースオーナー / クライアント / 認可サーバ / リソースサーバ）
- [ ] **OAuth 2.0 は「認可」の仕様、OpenID Connect (OIDC) が「認証」を足したもの**
- [ ] 認可コードフロー + PKCE（現在の推奨。Implicit フローは非推奨）
- [ ] `state` パラメータの役割（CSRF 対策）
- [ ] ID トークン（OIDC）とアクセストークンの違い

**課題**: Google Cloud Console で OAuth クライアントを作り、
ローカルの Next.js に「Google でログイン」を実装する。

---

## Step 5. 認可の設計（6〜8 時間）

認証ができたら、次は「誰が何をしてよいか」。

- [ ] RBAC（ロールベース）: user / editor / admin のようにロールで判定
- [ ] ABAC（属性ベース）: 「自分が作成した記事のみ編集可」など属性で判定
- [ ] 縦の権限昇格（一般ユーザ → 管理者）と横の権限昇格（他人のデータを閲覧）
- [ ] **IDOR**: `/posts/123` の 123 を書き換えて他人のデータが見えないか

実装の型:

```ts
// 悪い例: フロントでボタンを隠しただけ
{user.role === "admin" && <DeleteButton />}

// 良い例: サーバ側（API / Server Action）で必ず再チェック
if (session.user.role !== "admin") {
  return new Response("Forbidden", { status: 403 });
}
```

**課題**: 作成したアプリで「一般ユーザが他人の投稿を削除できないか」を
自分で攻撃して確認する（curl や DevTools で直接 API を叩く）。

---

## Step 6. ライブラリ / サービスを使う（6〜8 時間）

自作で仕組みを理解した後に、実務で使うものへ乗り換える。

| 選択肢                    | 特徴                                             |
| ------------------------- | ------------------------------------------------ |
| Auth.js (NextAuth.js)     | Next.js で定番。OAuth プロバイダが豊富            |
| Supabase Auth             | DB とセットで使える。Row Level Security と相性良し |
| Firebase Authentication   | 導入が速い。モバイルとも共通化しやすい            |
| Auth0 / Clerk             | SaaS。MFA や管理画面まで込みだが有料              |
| AWS Cognito               | AWS 構成に寄せるなら                              |

**課題**: Step 2〜5 で作ったアプリを Auth.js に置き換え、
自作時との差分（何を肩代わりしてくれているか）をメモする。

---

## Step 7. 攻撃と対策（継続的に）

- [ ] XSS: スクリプト混入 → トークン窃取。対策は出力エスケープ + `HttpOnly` Cookie + CSP
- [ ] CSRF: 意図しないリクエスト送信。対策は `SameSite` Cookie + CSRF トークン
- [ ] セッションフィクセーション: **ログイン成功時にセッション ID を再発行する**
- [ ] ブルートフォース / クレデンシャルスタッフィング: レート制限、アカウントロック
- [ ] ユーザ列挙: 「メールが存在しません」と教えない（エラーメッセージを統一する）
- [ ] 多要素認証 (MFA / TOTP)、パスキー (WebAuthn)

参考: OWASP Top 10、OWASP Cheat Sheet Series（Authentication / Session Management）

---

## 成果物（ポートフォリオ化）

最終的に以下が動く小さなアプリを 1 本作る。

1. メール + パスワードで新規登録・ログイン・ログアウト
2. Google ログイン（OIDC）
3. ログイン中のみアクセスできるページ
4. admin ロールのみ実行できる操作（サーバ側で判定）
5. 自分のリソースのみ編集・削除できる（IDOR 対策済み）

学んだことを記事化するとさらに定着する（zenn / note）。

---

## 進捗メモ

| Step | 内容                    | 状態 | メモ |
| ---- | ----------------------- | ---- | ---- |
| 0    | HTTP / Cookie           | 未   |      |
| 1    | パスワードとハッシュ    | 未   |      |
| 2    | セッション認証を自作    | 未   |      |
| 3    | トークン / JWT          | 未   |      |
| 4    | OAuth 2.0 / OIDC        | 未   |      |
| 5    | 認可設計 (RBAC/ABAC)    | 未   |      |
| 6    | Auth.js などへの移行    | 未   |      |
| 7    | 攻撃と対策              | 未   |      |

---

## 参考リンク

- MDN Web Docs: HTTP Cookie の使用 https://developer.mozilla.org/ja/docs/Web/HTTP/Cookies
- OWASP Cheat Sheet Series https://cheatsheetseries.owasp.org/
- OAuth 2.0 https://oauth.net/2/
- OpenID Connect https://openid.net/developers/how-connect-works/
- Auth.js https://authjs.dev/
- 書籍: 『Web ブラウザセキュリティ』『体系的に学ぶ 安全な Web アプリケーションの作り方（徳丸本）』
